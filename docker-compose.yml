version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: knowhy_postgres
    environment:
      POSTGRES_USER: knowhy
      POSTGRES_PASSWORD: knowhy_pass
      POSTGRES_DB: knowhy_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U knowhy"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: knowhy_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+asyncpg://knowhy:knowhy_pass@postgres:5432/knowhy_db
    volumes:
      - ./backend:/app
      - audio_uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: knowhy_frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      VITE_API_URL: http://localhost:8000
    depends_on:
      - backend
    stdin_open: true
    tty: true

volumes:
  postgres_data:
    name: knowhy_postgres_data
  audio_uploads:
    name: knowhy_audio_uploads

